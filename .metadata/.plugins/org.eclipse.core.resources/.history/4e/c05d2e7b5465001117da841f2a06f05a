from django.views.generic.simple import direct_to_template
from main.model import FLinks

i = 0

def show_mainpage(request):
    
    return direct_to_template(request, 'index.html' ,{'test': request['ID_SRC']});

def show_flinks(request):
    ''